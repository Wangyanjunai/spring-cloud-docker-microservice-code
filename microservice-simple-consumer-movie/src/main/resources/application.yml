server:
  port: 8010
management:
  endpoints:
    web:
      exposure:
        #如需暴露所有监控端点可配置 *
        include: '*'    
  endpoint:
    health:
      #展示显示健康详情
      show-details: always
